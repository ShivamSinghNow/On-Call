{
  "name": "voice-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Voice Bridge MVP: React Native app -> on-device Cactus/Gemma STT -> Node bridge -> Telegram/Claude Code -> on-device TTS",
  "scripts": {
    "bridge": "pnpm --filter @voice-bridge/bridge dev",
    "mobile": "pnpm --filter @voice-bridge/mobile start",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
